# Copy to .env.local and adjust per environment (Vercel: set in Project Settings → Environment Variables).
NEXT_PUBLIC_BACKEND_URL=http://localhost:5001

# Tạm thời bỏ login học viên (vào dashboard thẳng). Bật lại: false + npm run build
NEXT_PUBLIC_AUTH_DISABLED=true
